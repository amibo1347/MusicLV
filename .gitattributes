# 기본은 자동 판별하되, 저장소에는 LF 로 넣는다.
* text=auto eol=lf

# 리눅스 서버에서 실행되는 스크립트는 반드시 LF 여야 한다.
# CRLF 로 체크아웃되면 "bad interpreter" 로 배포가 깨진다.
*.sh    text eol=lf
gradlew text eol=lf

# Windows 전용 스크립트는 CRLF 유지
*.bat   text eol=crlf

# 바이너리
*.jar   binary
*.png   binary
*.jpg   binary
*.mp4   binary
